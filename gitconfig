[core]
    excludesfile = ~/.gitignore
    editor = /usr/bin/vim -i NONE -c \"set fenc=utf-8\"
    whitespace = trailing-space,space-before-tab
    ignorecase = false

[color]
    ui = auto

[alias]
    br = branch
    bv = branch -vv
    bva = branch -vv --all
    ci = commit
    co = checkout
    df = diff
    lgp = log -p
    glo = log --graph --date-order
    gl1 = log --graph --date-order --oneline
    gla = log --graph --date-order --oneline --all
    st = status --branch
    pf = push --force-with-lease

[diff]
    comapctionHeuristic = true

[push]
    default = current

[fetch]
    prune = true

[credential]
    helper = osxkeychain

[pager]
    log = diff-highlight | less
    show = diff-highlight | less
    diff = diff-highlight | less

[includeIf "gitdir:~/work/gh/corp/"]
    path = ~/.gitconfig.corp

[includeIf "gitdir:~/work/gh/shkumagai/"]
    path = ~/.gitconfig.shkumagai
