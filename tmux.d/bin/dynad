#!/bin/bash

CWD=$(cd $(dirname $0); pwd)
TMUX_ROOT=$(cd $CWD/..; pwd)

bash ${TMUX_ROOT}/bin/dynad-kvs-user
bash ${TMUX_ROOT}/bin/dynad-kvs-item
bash ${TMUX_ROOT}/bin/dynad-datafeed
bash ${TMUX_ROOT}/bin/dynad-logmanage
